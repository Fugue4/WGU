<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_centerVertical="true"
    android:id="@+id/assessment_list_text_layout">

    <TextView
        android:id="@+id/assessment_list_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp"
        android:paddingTop="10dp"
        android:textAppearance="@style/TextAppearance.AppCompat.Title"
        tools:text="Title" />

    <TextView
        android:id="@+id/assessment_list_due_caption"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/assessment_list_title"
        android:layout_alignStart="@+id/assessment_list_title"
        android:layout_below="@+id/assessment_list_title"
        android:text="@string/due"
        android:textAppearance="@style/TextAppearance.AppCompat.Caption" />

    <TextView
        android:id="@+id/assessment_list_due"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/assessment_list_due_caption"
        android:layout_alignBottom="@+id/assessment_list_due_caption"
        android:layout_marginLeft="3dp"
        android:layout_marginStart="3dp"
        android:layout_toEndOf="@+id/assessment_list_due_caption"
        android:layout_toRightOf="@+id/assessment_list_due_caption"
        android:ellipsize="end"
        android:maxLines="3"
        android:textAppearance="@style/TextAppearance.AppCompat.Medium"
        tools:text="Due" />


</RelativeLayout>